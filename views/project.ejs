<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <title><%= project.title %></title>
</head>
<body>
  <%- include('partials/navbar', { page: '' }) %>

  <section class="container mx-auto my-8">
    <div class="text-center">
      <h1 class="text-4xl font-bold"><%= project.title %></h1>
      <p class="mt-4">Below, you will find detailed information about the project: <%= project.title %></p>
    </div>

    <div class="my-8">
      <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="w-full h-auto mb-4">
      <p><strong>Intro:</strong> <%= project.intro_short %></p>
      <p><strong>Impact:</strong> <%= project.impact %></p>
      <a href="<%= project.original_source_url %>" target="_blank" class="text-blue-500">More Information</a>
    </div>

    <p id="quote" class="italic text-center mt-8">Fetching quote...</p>
    <a href="#" onclick="history.back(); return false;" class="text-blue-500">Back</a>

    <script>
      document.addEventListener("DOMContentLoaded", () => {
        fetch("https://dummyjson.com/quotes/random")
          .then(response => response.json())
          .then(data => {
            document.getElementById("quote").innerText = `"${data.quote}" - ${data.author}`;
          });
      });
    </script>
  </section>
</body>
</html>
